{
  "name": "brix-content-pipeline",
  "version": "1.0.0",
  "description": "Automated LEGO investment content pipeline for BRIX - generates carousel posts for TikTok & Instagram",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "generate": "node src/index.js",
    "test": "node src/index.js --dry-run"
  },
  "dependencies": {
    "puppeteer": "^23.0.0",
    "@anthropic-ai/sdk": "^0.30.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
